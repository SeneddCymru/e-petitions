<%= link_to "Back", admin_domains_path, class: "back-link" %>

<h1>New Domain</h1>

<div class="grid-row">
  <div class="column-two-thirds extra-gutter">
    <%= form_for [:admin, @domain] do |form| %>
      <%= render 'form', form: form %>
    <% end %>
  </div>
</div>
