<section aria-labelledby="page-heading">
  <% if Site.show_feedback_page_message? %>
    <div class="notification important">
      <%= markdown_to_html(Site.feedback_page_message) %>
    </div>
  <% end %>

  <%= t(:"ui.contact_page.body_html") %>
</section>
